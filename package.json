{
  "name": "aha-mcp",
  "version": "0.1.1",
  "description": "MCP server for Aha! — productivity-focused ticket querying",
  "type": "module",
  "bin": {
    "aha-mcp": "./src/index.ts"
  },
  "files": [
    "src/",
    "README.md"
  ],
  "engines": {
    "bun": ">=1.0.0"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.2",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.4.5"
  }
}
